/**
 * domain/roi/ROICalculatorInput.ts
 *
 * Estado del slider interactivo — lo único que el usuario controla.
 */
export interface ROICalculatorInput {
  /** Cantidad de tragos que el bar reporta vender por fin de semana (valor del slider) */
  tragosPerFinDeSemana: number;
}