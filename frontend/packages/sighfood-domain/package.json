{
  "name": "@sighfood/domain",
  "version": "1.0.0",
  "description": "Reglas de negocio puras de SIGH_FOOD - agrupa todo lo que cambia por la misma razón de negocio (CCP)",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "test": "vitest",
    "test:watch": "vitest --watch",
    "build": "tsc",
    "lint": "eslint ."
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.2.0",
    "@types/node": "^20.11.0"
  },
  "peerDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}